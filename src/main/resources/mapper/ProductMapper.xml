<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xvxiang.productsalessystem.mapper.ProductMapper">

    <resultMap id="BaseResultMap" type="com.xvxiang.productsalessystem.entity.Product">
        <id column="id" property="id"/>
        <result column="product_code" property="productCode"/>
        <result column="name" property="name"/>
        <result column="category_id" property="categoryId"/>
        <result column="price" property="price"/>
        <result column="cost" property="cost"/>
        <result column="stock" property="stock"/>
        <result column="unit" property="unit"/>
        <result column="brand" property="brand"/>
        <result column="model" property="model"/>
        <result column="specification" property="specification"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="image_url" property="imageUrl"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="ProductWithCategoryMap" type="com.xvxiang.productsalessystem.entity.Product" extends="BaseResultMap">
        <result column="category_name" property="categoryName"/>
    </resultMap>

    <sql id="whereClause">
        <where>
            <if test="productCode != null and productCode != ''">
                AND p.product_code LIKE CONCAT('%', #{productCode}, '%')
            </if>
            <if test="name != null and name != ''">
                AND p.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                AND p.category_id = #{categoryId}
            </if>
            <if test="status != null">
                AND p.status = #{status}
            </if>
        </where>
    </sql>

    <select id="selectPage" resultMap="ProductWithCategoryMap">
        SELECT p.*, c.name AS category_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id
        <include refid="whereClause"/>
        ORDER BY p.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM product p
        <include refid="whereClause"/>
    </select>

    <select id="selectById" resultMap="ProductWithCategoryMap">
        SELECT p.*, c.name AS category_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.id = #{id}
    </select>

    <select id="selectByProductCode" resultMap="BaseResultMap">
        SELECT * FROM product WHERE product_code = #{productCode}
    </select>

    <insert id="insert" parameterType="com.xvxiang.productsalessystem.entity.Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (
            product_code, name, category_id, price, cost, stock, unit, brand, model, 
            specification, description, status, image_url
        ) VALUES (
            #{productCode}, #{name}, #{categoryId}, #{price}, #{cost}, #{stock}, #{unit}, 
            #{brand}, #{model}, #{specification}, #{description}, #{status}, #{imageUrl}
        )
    </insert>

    <update id="update" parameterType="com.xvxiang.productsalessystem.entity.Product">
        UPDATE product
        <set>
            <if test="productCode != null">product_code = #{productCode},</if>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="cost != null">cost = #{cost},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="brand != null">brand = #{brand},</if>
            <if test="model != null">model = #{model},</if>
            <if test="specification != null">specification = #{specification},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
        </set>
        WHERE id = #{id}
    </update>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO product (
            product_code, name, category_id, price, cost, stock, unit, brand, model,
            specification, description, status, image_url
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.productCode}, #{item.name}, #{item.categoryId}, #{item.price}, #{item.cost},
                #{item.stock}, #{item.unit}, #{item.brand}, #{item.model}, #{item.specification},
                #{item.description}, #{item.status}, #{item.imageUrl}
            )
        </foreach>
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <delete id="deleteByIds" parameterType="java.util.List">
        DELETE FROM product WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
